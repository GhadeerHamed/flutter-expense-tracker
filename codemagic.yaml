workflows:
  ios-release-no-shorebird:
    name: iOS Release
    environment:
      flutter: stable
      xcode: latest

    scripts:
      - flutter build ipa --release --no-shorebird

    artifacts:
      - build/ios/ipa/*.ipa